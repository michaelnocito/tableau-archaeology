# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# Node (only if tooling is added later)
node_modules/
npm-debug.log*

# Playwright (test runner output — keep the specs + config, not the artifacts)
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Local notes never meant for the public repo
*.local.json
NOTES.local.md
PLAYTEST.md
BLEND_PLAN.md
